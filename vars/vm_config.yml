---
# ===============================================================================
# IMPORTANT: This file is AUTO-GENERATED from your .env file
# ===============================================================================
#
# DO NOT edit this file directly! Your changes will be overwritten.
#
# To update configuration:
#   1. Edit your .env file with your desired settings
#   2. Run: make generate-config
#   3. This file will be regenerated with values from .env
#
# If you don't have a .env file yet:
#   cp .env.example .env
#   # Edit .env with your settings
#   make generate-config
#
# ===============================================================================

# This is a placeholder template. Run 'make generate-config' to populate it.
# The script will read from your .env file and generate this file with:
#
# - Proxmox API Configuration (host, user, password, node)
# - OpenSUSE Image Path (where your custom image is located)
# - GitHub SSH Key Configuration (optional username for key import)
# - Storage Configuration (pools, disk sizes)
# - Network Configuration (bridges for public/private networks)
# - VM Default Settings (memory, CPU, cores)
# - VM Definitions (up to 4 VMs with customizable settings)
#
# All values come from environment variables in your .env file.
# See .env.example for all available configuration options.

# Minimal configuration to prevent errors if not yet generated:
proxmox_api_user: "root@pam"
proxmox_api_password: "CHANGEME"
proxmox_api_host: "proxmox.example.com"
proxmox_node: "pve"
opensuse_image_path: "/var/lib/vz/template/iso/opensuse-leap-custom.qcow2"
github_username: ""
vm_root_password: "opensuse"
storage_pool: "local-lvm"
data_disk_size: "1000G"
mon_disk_size: "100G"
private_bridge: "vmbr1"
public_bridge: "vmbr0"
vm_default_memory: 32768
vm_default_cores: 8
vm_default_sockets: 1
vm_cpu_type: "host"
auto_start: true
vms:
  - name: "ceph-node1"
    vmid: 200
    memory: 32768
    cores: 8
    sockets: 1
    onboot: 1
    ip: "192.168.1.10"

# REMINDER: Run 'make generate-config' to populate with your actual values!
